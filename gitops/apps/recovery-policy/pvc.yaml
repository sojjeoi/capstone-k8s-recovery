apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: recovery-policy-data
  namespace: vllm-serving
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-storage
  resources:
    requests:
      storage: 1Gi
