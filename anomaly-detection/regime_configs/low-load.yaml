# 정상 상태 데이터셋용 — 저부하 구간 (ramp.py 재사용)
target:
  url: "http://vllm-active.vllm-serving.svc.cluster.local:8000/v1/completions"
  model: "Qwen/Qwen2.5-0.5B-Instruct"
  prompt: "Hello"
  max_tokens: 10

stages:
  - name: normal-low-load
    rps: 1
    duration_sec: 60
